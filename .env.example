# Public (safe to commit placeholders)
NEXT_PUBLIC_GHL_BOOKING_URL=
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Payload CMS — set only in Vercel / .env.local (never commit real values)
PAYLOAD_SECRET=

# Neon Postgres (auto-injected by Vercel Marketplace for tmt-db)
DATABASE_URL=
POSTGRES_URL=

# Vercel Blob (auto-injected for tmt-media) — public website media only
BLOB_READ_WRITE_TOKEN=
